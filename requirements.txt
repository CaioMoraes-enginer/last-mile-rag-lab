reportlab>=4.0,<5
# Ingestao: parsing layout-aware + estrutura de tabela (KAN-16).
docling>=2.120,<3
# pdfplumber: extrator baseline do KAN-6 (mantido para comparacao no KAN-10).
pdfplumber>=0.11,<1
pypdf>=5,<7
# Banco: acesso ao Postgres + pgvector (KAN-15)
psycopg[binary]>=3.2,<4
pgvector>=0.3,<1
python-dotenv>=1,<2
# Modelo de dominio e contrato de decisao (KAN-3)
pydantic>=2,<3
# Testes ouro e contrafactuais (KAN-5)
pytest>=8,<9